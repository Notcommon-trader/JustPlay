name: jp_core
description: Pure Dart game rules and scoring. No Flutter dependency, by design.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.12.0

resolution: workspace

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
